Finally, the evidence screen summarizes the independent tests. The reply Skill passes output-shape and numeric-grounding checks. The quality Skill passes score-range, cap, arithmetic, and evidence checks. The retention Agent traces all three sources without converting hypotheses into facts. The bug Agent rejects contradictory near matches and stays within the supplied evidence.

The full application build, server-render test, and demo API test pass. The repository also includes every Skill package, Agent instruction file, input schema, realistic fixture, expected output, AI-use disclosure, and the rendered PDF. What is intentionally absent is just as important: no orchestration, no scheduler, no background polling, no ticket mutation, and no full pipeline demo. That is Support Lab: the smallest trustworthy component for each job.
